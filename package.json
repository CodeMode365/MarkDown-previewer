{
  "name": "markdown-previewer",
  "author": "CodeMode365(Pabin)",
  "description": "Markdown previewer as the freeCodeCamp frontend library certification project",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.0.0"
  }
}
